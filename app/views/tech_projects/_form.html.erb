<%= form_with(model: tech_project, local: true) do |form| %>
  <% if tech_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tech_project.errors.count, "error") %> prohibited this tech_project from being saved:</h2>

      <ul>
        <% tech_project.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :tech_image %>
    <%= form.text_field :tech_image %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
